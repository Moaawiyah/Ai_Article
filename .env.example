# Copy this file to .env and fill in real values. Never commit .env.

# Article pipeline — provider is selected in config/config.yaml (llm.provider).
# Set the key matching the active provider:
ZHIPUAI_API_KEY=your-zhipuai-api-key-here

# Document Q&A path (sdk.query_document):
ANTHROPIC_API_KEY=your-anthropic-api-key-here

APP_ENV=development
LOG_LEVEL=INFO
